NEXT_PUBLIC_API_URL="https://api.myhome.co.id"
# Public R2 custom domain used by browser-facing image URLs.
NEXT_PUBLIC_R2_PUBLIC_BASE_URL="https://assets.myhome.co.id"
# Backward-compatible fallback if you still prefer hostname-only config.
NEXT_PUBLIC_STORAGE_HOSTNAME=""
